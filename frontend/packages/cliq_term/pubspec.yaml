name: cliq_term
description: "A new Flutter package project."
version: 0.0.1
homepage:

environment:
  sdk: 3.11.0
  flutter: ">=3.35.7"
resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  logging: ^1.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  test: ^1.25.6
