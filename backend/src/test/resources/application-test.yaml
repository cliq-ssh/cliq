app.keycloak:
  url: "http://localhost:8100"
  username: "admin"
  password: "cliq"

spring:
  config:
    import: optional:file:.env[.properties],optional:file:.env.local[.properties],optional:file:.env.test[.properties],optional:file:.env.test.local[.properties]
  test:
    database:
      replace: none
