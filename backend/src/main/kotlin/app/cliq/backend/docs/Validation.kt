package app.cliq.backend.docs

// Passwords
const val MIN_PASSWORD_LENGTH = 8
const val MAX_PASSWORD_LENGTH = 256

// Jwt
const val MIN_JWT_EXPIRES_MINUTES = 5L
const val MIN_JWT_REFRESH_TOKEN_EXPIRES_DAYS = 1L
